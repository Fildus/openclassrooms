<!doctype html>
<html lang="fr">
<head>
    {{ include('HeadAdmin.twig') }}
</head>
<body>
{{ include('ModalConnexion.twig') }}
{{ include('ModalLogin.twig') }}
{{ include('ModalSubscribe.twig') }}
{{ include('ModalUnsubscribe.twig') }}
{% if session %}
    {{ include('ModalCompte.twig') }}
{% endif %}

<div class="container">
	<div class="row">
		<div class="col-12">
			<div class="background">

				<header>
                    {{ include('Header.twig') }}
				</header>

				<main role="main">
                    {{ block('content') }}
				</main>

			</div>
		</div>
	</div>
</div>

<footer class="container-fluid footer">
    {{ include('NavAdmin.twig') }}
</footer>

{{ include('CssJsAdmin.twig') }}
</body>
</html>